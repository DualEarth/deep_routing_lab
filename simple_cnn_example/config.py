# config.py

# Image size for the input images
image_size = 8

# Number of images in the dataset
num_images = 1000
num_images_train = int(num_images*0.7)

# Size of the network (complexity of model)
hidden_size = 16

# batch size
batch_size = 16

num_epochs = 64

kernel_size = 3